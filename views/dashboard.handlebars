{{#if posts.length}}
    {{#each posts as |post|}}
    <div class="card">

        <div class="card-body">
            <h5 class="card-title">{{post.title}}</h5>
            <p class="card-text">{{post.text}}</p>
            <p class="card-text">{{post.date_posted}},{{post.id}}</p>
        </div>
        <a href="/dashboard/post/{{id}}"><img src="https://www.freeiconspng.com/img/28554" alt="edit"></a>
    </div>
    {{/each}}
{{else}}
    <div class="card">
  <div class="card-header">
    Featured
  </div>
  <div class="card-body">
    <h5 class="card-title">There are no posts</h5>
    <p class="card-text">Make a new one and get started!</p>
  </div>
</div>
{{/if}}