<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link
      href="https://fonts.googleapis.com/css2?family=Amatic+SC&family=Montserrat&family=Over+the+Rainbow&display=swap"
      rel="stylesheet">
    <link rel="stylesheet" href="/css/jass.css" />
</head>

<body>
    <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between px-2 py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-dark text-decoration-none fs-1">Tech Blog
        </a>

        <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
        <li><a href="/" class="nav-link px-2 link-secondary">Home</a></li>
        {{#if loggedIn}}
        <li><a href="/dashboard" class="nav-link px-2 link-dark">Dashboard</a></li>
        <li><a href="/dashboard/posts/" class="nav-link px-2 link-dark">Create Post</a></li>
        {{/if}}
        </ul>

        <div class="col-md-3 text-end">
            {{#if loggedIn}}
            <button type="button" id="logout" class="btn btn-outline-primary me-2">Logout</button>
            {{else}}
            <a href="/login" class="btn btn-outline-primary me-2">Login</a>
            {{/if}}
        </div>
  </header>

  <main>
    <div class="">
        {{{body}}}
    </div>
  </main>

  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{else}}
  {{!-- <script src="/js/login.js"></script> --}}
    {{/if}}
</body>
</html>